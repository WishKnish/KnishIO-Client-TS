# nodeLinker: yarn 4 defaults to PnP, which would silently wipe node_modules (July 2026 lesson).
nodeLinker: node-modules

# ChainDrop response (2026-08): quarantine npm versions younger than 7 days (unit: minutes).
# This repo is a standalone clone in CI, so the key must live HERE, not only in the
# monorepo root's .yarnrc.yml. Release-day escape: YARN_NPM_MINIMAL_AGE_GATE=0 yarn <cmd>.
npmMinimalAgeGate: 10080

# The 2026-08 accept of 1120680 (GHSA-g7r4-m6w7-qqqr, esbuild <0.28.1 dev-server arbitrary file
# read) is retired: esbuild is pinned to ^0.28.1 through `resolutions`/`overrides`, so the
# advisory no longer applies and the entry would only have hidden a future recurrence. Removing
# it changes no audit result today — `yarn npm audit --environment all --recursive` reports the
# same findings with and without it (verified 2026-09-21).

yarnPath: .yarn/releases/yarn-4.15.0.cjs
